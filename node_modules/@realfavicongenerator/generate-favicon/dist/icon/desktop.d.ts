import { Svg } from "@svgdotjs/svg.js";
import { IconTransformation, MasterIcon } from "./helper";
export type DesktopIconSettings = {
    regularIconTransformation: IconTransformation;
    darkIconType: 'none' | 'regular' | 'specific';
    darkIconTransformation: IconTransformation;
};
export declare const initDesktopIconSettings: () => DesktopIconSettings;
export declare const getAppliedDarkIcon: (masterIcon: MasterIcon, settings: DesktopIconSettings) => {
    darkIcon: Svg;
    darkIconTransformation: IconTransformation;
};
export declare const hasDarkIcon: (settings: DesktopIconSettings) => boolean;
