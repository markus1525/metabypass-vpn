"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const _1 = require(".");
test('normalizeFaviconPath', () => {
    expect((0, _1.normalizeFaviconPath)('/')).toEqual('/');
    expect((0, _1.normalizeFaviconPath)('')).toEqual('/');
    expect((0, _1.normalizeFaviconPath)('/my-favicon')).toEqual('/my-favicon/');
    expect((0, _1.normalizeFaviconPath)('/my-favicon/')).toEqual('/my-favicon/');
    expect((0, _1.normalizeFaviconPath)('my-favicon')).toEqual('/my-favicon/');
    expect((0, _1.normalizeFaviconPath)('/path/to/favicon')).toEqual('/path/to/favicon/');
    expect((0, _1.normalizeFaviconPath)('/path/to/favicon/')).toEqual('/path/to/favicon/');
    expect((0, _1.normalizeFaviconPath)('path/to/favicon')).toEqual('/path/to/favicon/');
});
